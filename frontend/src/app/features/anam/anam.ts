import { DatePipe } from '@angular/common';
import { Component, computed, inject, OnDestroy, OnInit, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute } from '@angular/router';
import { ChatMessage } from '../../models/anam.model';
import { AnamService } from '../../services/anam.service';
import { SessionsService } from '../../services/sessions.service';
import { PageHeader } from '../../shared/components/page-header/page-header';

@Component({selector:'app-anam-trainer',standalone:true,imports:[FormsModule,DatePipe,PageHeader],template:`
  <app-page-header eyebrow="Intelligence artificielle" title="Coach IA Anam" description="Pratiquez a voix haute, posez vos questions et avancez avec un coach disponible dans le contexte de votre session." />
  <div class="trainer-layout">
    <section class="avatar-stage" [class.live]="anam.active()">
      <video id="anam-avatar-video" autoplay playsinline aria-label="Avatar du coach IA Anam"></video>
      @if(!anam.active()){
        <div class="avatar-placeholder"><div class="avatar-face"><span class="material-symbols-rounded">face_6</span><i></i></div><h2>{{anam.connecting()?'Connexion a votre coach...':'Votre coach est pret'}}</h2><p>{{anam.connecting()?'Initialisation de la session securisee Anam.ai.':'Selectionnez une session et lancez la conversation.'}}</p>@if(anam.connecting()){<div class="connecting"><span></span><span></span><span></span></div>}</div>
      }
      <div class="stage-top"><span class="status"><i></i>{{anam.active()?'En conversation':'Hors ligne'}}</span><button type="button" aria-label="Plein ecran" title="Plein ecran" (click)="fullScreen()"><span class="material-symbols-rounded">fullscreen</span></button></div>
      <div class="stage-controls">
        @if(!anam.active()){<button class="start" type="button" (click)="start()" [disabled]="anam.connecting()||!selectedSessionId()"><span class="material-symbols-rounded">play_arrow</span>Demarrer la conversation</button>}
        @else{<button class="round" type="button" [class.muted]="muted()" (click)="toggleMuted()" [attr.aria-label]="muted()?'Activer le micro':'Couper le micro'"><span class="material-symbols-rounded">{{muted()?'mic_off':'mic'}}</span></button><button class="end" type="button" (click)="end()"><span class="material-symbols-rounded">call_end</span>Terminer</button>}
      </div>
    </section>
    <aside class="chat-panel">
      <header><div><h2>Conversation</h2><p>{{currentSessionTitle()}}</p></div><button type="button" title="Effacer l'historique" aria-label="Effacer l'historique" (click)="clearHistory()"><span class="material-symbols-rounded">delete_sweep</span></button></header>
      <div class="context-select"><label for="session-select">Session de formation</label><select id="session-select" [ngModel]="selectedSessionId()" (ngModelChange)="selectedSessionId.set($event)" [disabled]="anam.active()"><option value="">Choisir une session</option>@for(session of sessions.sessions();track session.id){<option [value]="session.id">{{session.title}}</option>}</select></div>
      <div class="messages" aria-live="polite">
        @for(message of allMessages();track message.id){<div class="message" [class.learner]="message.role==='learner'" [class.system]="message.role==='system'"><span class="author">{{message.role==='learner'?'Vous':message.role==='trainer'?'Coach Anam':'Forma'}}</span><p>{{message.content}}</p><time>{{message.createdAt|date:'HH:mm'}}</time></div>}
        @if(anam.connecting()){<div class="typing" aria-label="Le coach se connecte"><i></i><i></i><i></i></div>}
      </div>
      @if(anam.error()){<div class="anam-error" role="alert"><span class="material-symbols-rounded">error</span>{{anam.error()}}</div>}
      <form class="composer" (ngSubmit)="submitMessage()"><textarea name="prompt" rows="2" [ngModel]="prompt()" (ngModelChange)="prompt.set($event)" [disabled]="anam.connecting()" placeholder="Ecrire un message au coach..." aria-label="Votre message au coach"></textarea><button class="voice" type="button" (click)="dictate()" [class.listening]="listening()" [disabled]="anam.connecting()" title="Dicter le message" aria-label="Dicter le message"><span class="material-symbols-rounded">mic</span></button><button class="send" type="submit" [disabled]="anam.connecting()||!prompt().trim()||(!anam.active()&&!selectedSessionId())" title="Envoyer" aria-label="Envoyer le message"><span class="material-symbols-rounded">arrow_upward</span></button></form>
      <footer><span class="material-symbols-rounded">shield</span>Le jeton Anam est genere de maniere securisee par votre API.</footer>
    </aside>
  </div>
`,styles:[`
  .trainer-layout{height:calc(100vh - 192px);min-height:620px;display:grid;grid-template-columns:minmax(0,1.65fr) minmax(340px,.75fr);gap:16px}.avatar-stage{min-width:0;position:relative;overflow:hidden;border-radius:8px;background:#23262e;color:white}.avatar-stage:before{content:'';position:absolute;inset:0;background:linear-gradient(145deg,#20222a,#353957 56%,#1d625c)}.avatar-stage video{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;opacity:0;transition:opacity .35s ease}.avatar-stage.live video{opacity:1}.avatar-placeholder{position:absolute;inset:0;z-index:1;display:grid;place-items:center;align-content:center;text-align:center}.avatar-face{width:170px;height:170px;position:relative;display:grid;place-items:center;border-radius:50%;background:rgba(255,255,255,.09);border:1px solid rgba(255,255,255,.18);box-shadow:0 0 0 34px rgba(255,255,255,.035),0 0 0 72px rgba(255,255,255,.02)}.avatar-face>span{font-size:90px;color:#a7a9ff}.avatar-face i{position:absolute;right:12px;bottom:18px;width:22px;height:22px;border:5px solid #2c2f39;border-radius:50%;background:#6fe0be}.avatar-placeholder h2{margin:58px 0 8px;font-size:1.1rem}.avatar-placeholder p{margin:0;color:rgba(255,255,255,.6);font-size:.76rem}.connecting{display:flex;gap:5px;margin-top:18px}.connecting span,.typing i{width:6px;height:6px;border-radius:50%;background:#a8aaff;animation:bounce 1s infinite}.connecting span:nth-child(2),.typing i:nth-child(2){animation-delay:.14s}.connecting span:nth-child(3),.typing i:nth-child(3){animation-delay:.28s}.stage-top{position:absolute;inset:18px 18px auto;z-index:3;display:flex;justify-content:space-between}.status{padding:7px 10px;display:flex;align-items:center;gap:6px;border-radius:5px;background:rgba(20,22,28,.58);font-size:.65rem;backdrop-filter:blur(8px)}.status i{width:6px;height:6px;border-radius:50%;background:#8c909c}.live .status i{background:#66dfbd}.stage-top button,.round{width:38px;height:38px;border:1px solid rgba(255,255,255,.16);border-radius:6px;display:grid;place-items:center;background:rgba(20,22,28,.5);color:white;cursor:pointer;backdrop-filter:blur(8px)}.stage-controls{position:absolute;left:0;right:0;bottom:24px;z-index:3;display:flex;justify-content:center;gap:10px}.stage-controls button{min-height:44px;border:0;border-radius:7px;display:flex;align-items:center;justify-content:center;gap:7px;color:white;font-size:.73rem;font-weight:700;cursor:pointer}.stage-controls button:disabled{opacity:.45;cursor:not-allowed}.start{padding:0 18px;background:#6f71e8}.end{padding:0 17px;background:#cf4e52}.stage-controls .round{width:44px;background:rgba(20,22,28,.68)}.round.muted{background:#cf4e52}.chat-panel{min-width:0;display:flex;flex-direction:column;border:1px solid var(--border);border-radius:8px;background:var(--surface);overflow:hidden}.chat-panel>header{min-height:68px;padding:14px 16px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--border)}.chat-panel header h2{margin:0;font-size:.9rem}.chat-panel header p{max-width:230px;margin:5px 0 0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:var(--muted);font-size:.66rem}.chat-panel header button{width:34px;height:34px;border:0;border-radius:5px;display:grid;place-items:center;background:transparent;color:var(--muted);cursor:pointer}.chat-panel header button:hover{background:var(--surface-2)}.context-select{padding:12px 16px;border-bottom:1px solid var(--border);display:grid;gap:5px}.context-select label{color:var(--muted);font-size:.62rem;font-weight:600}.context-select select{width:100%;height:36px;padding:0 8px;border:1px solid var(--border);border-radius:5px;background:var(--surface);color:var(--ink);font-size:.72rem}.messages{min-height:0;flex:1;padding:18px 16px;overflow:auto;display:flex;flex-direction:column;gap:14px}.message{max-width:88%;align-self:flex-start}.message.learner{align-self:flex-end}.author{display:block;margin:0 6px 5px;color:var(--muted);font-size:.6rem}.message p{margin:0;padding:10px 12px;border-radius:7px 7px 7px 2px;background:var(--surface-2);font-size:.73rem;line-height:1.55}.message.learner p{background:var(--brand);color:white;border-radius:7px 7px 2px 7px}.message.system{max-width:100%;align-self:center;text-align:center}.message.system p{padding:7px 10px;background:transparent;color:var(--muted);font-size:.65rem}.message time{display:block;margin:4px 6px 0;color:var(--muted);font-size:.55rem}.message.learner time{text-align:right}.typing{width:max-content;padding:10px 12px;display:flex;gap:4px;border-radius:7px;background:var(--surface-2)}.anam-error{margin:0 14px 8px;padding:9px;display:flex;align-items:flex-start;gap:6px;border-radius:5px;background:color-mix(in srgb,var(--danger) 10%,transparent);color:var(--danger);font-size:.65rem;line-height:1.4}.anam-error span{font-size:17px}.composer{margin:0 12px 10px;padding:7px;display:grid;grid-template-columns:1fr auto auto;align-items:end;gap:5px;border:1px solid var(--border);border-radius:7px;background:var(--surface-2)}.composer textarea{min-width:0;max-height:90px;padding:5px;border:0;outline:0;resize:none;background:transparent;color:var(--ink);font-size:.72rem;line-height:1.45}.composer button{width:34px;height:34px;border:0;border-radius:5px;display:grid;place-items:center;cursor:pointer}.voice{background:transparent;color:var(--muted)}.voice.listening{color:var(--danger);animation:pulse 1s infinite}.send{background:var(--ink);color:var(--surface)}.composer button:disabled{opacity:.35}.composer span{font-size:18px}.chat-panel footer{padding:0 14px 12px;display:flex;align-items:center;gap:5px;color:var(--muted);font-size:.57rem}.chat-panel footer span{font-size:14px;color:var(--teal)}@keyframes bounce{50%{transform:translateY(-4px);opacity:.55}}@keyframes pulse{50%{opacity:.5}}@media(max-width:1050px){.trainer-layout{height:auto;grid-template-columns:1fr}.avatar-stage{min-height:540px}.chat-panel{min-height:520px}}@media(max-width:600px){.trainer-layout{min-height:0}.avatar-stage{min-height:450px}.avatar-face{width:130px;height:130px}.avatar-face>span{font-size:68px}.avatar-placeholder h2{margin-top:44px}}
`]})
export class AnamTrainer implements OnInit,OnDestroy{readonly anam=inject(AnamService);readonly sessions=inject(SessionsService);private readonly route=inject(ActivatedRoute);readonly selectedSessionId=signal(this.route.snapshot.paramMap.get('sessionId')??'');readonly prompt=signal('Je souhaite revoir les notions importantes de cette formation et pratiquer avec des questions concretes.');readonly muted=signal(false);readonly listening=signal(false);readonly messages=signal<ChatMessage[]>([{id:'welcome',role:'trainer',content:'Bonjour, je suis votre coach Anam. Choisissez une session et dites-moi ce que vous souhaitez travailler.',createdAt:new Date()}]);readonly allMessages=computed(()=>[...this.messages(),...this.anam.transcript()]);readonly currentSessionTitle=computed(()=>this.sessions.sessions().find(s=>s.id===this.selectedSessionId())?.title??'Selectionnez une session');ngOnInit():void{this.sessions.load();}ngOnDestroy():void{this.anam.stop();}async start():Promise<void>{if(!this.selectedSessionId()||this.anam.active()||this.anam.connecting())return;const text=this.prompt().trim()||'Aidez-moi a reviser cette formation.';this.messages.update(items=>[...items,{id:crypto.randomUUID(),role:'learner',content:text,createdAt:new Date()}]);this.prompt.set('');try{await this.anam.start(this.selectedSessionId(),text,'anam-avatar-video');this.messages.update(items=>[...items,{id:crypto.randomUUID(),role:'system',content:'Conversation vocale demarree. Parlez naturellement a votre coach.',createdAt:new Date()}]);}catch{}}submitMessage():void{if(!this.anam.active()){void this.start();return;}const content=this.prompt().trim();if(!content)return;this.anam.sendMessage(content);this.prompt.set('');}toggleMuted():void{const next=!this.muted();this.muted.set(next);this.anam.setMuted(next);}end():void{this.anam.stop();this.muted.set(false);this.messages.update(items=>[...items,{id:crypto.randomUUID(),role:'system',content:'Conversation terminee.',createdAt:new Date()}]);}clearHistory():void{this.messages.set([]);this.anam.transcript.set([]);}fullScreen():void{const element=document.querySelector('.avatar-stage');void element?.requestFullscreen?.();}dictate():void{const SpeechRecognition=(window as unknown as {SpeechRecognition?:new()=>any;webkitSpeechRecognition?:new()=>any}).SpeechRecognition??(window as unknown as {webkitSpeechRecognition?:new()=>any}).webkitSpeechRecognition;if(!SpeechRecognition)return;const recognition=new SpeechRecognition();recognition.lang='fr-FR';recognition.interimResults=false;this.listening.set(true);recognition.onresult=(event:any)=>this.prompt.set(event.results[0][0].transcript);recognition.onend=()=>this.listening.set(false);recognition.onerror=()=>this.listening.set(false);recognition.start();}}
